// The conplexity of the following code is base on the variable arr

// Case sorted ascending array like [1, 2, 3, 4, 5]
// The number of loop times will be n => O(N)

// Case sorted descending array like [5, 4, 3, 2, 1]
// The number of loop times will be (n*(n+1))/2 => O(N^2)

// When the array is random (unsort) we cannot calculate the complexity for
// that general case